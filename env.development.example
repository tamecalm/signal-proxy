# Signal Proxy - Development Environment
# Copy this file to .env and customize as needed

# Environment: development or production
APP_ENV=development

# ============================================
# Tunnel Configuration
# ============================================
# Choose your tunnel provider: "ngrok", "cloudflare", "none", or "auto"
# - auto: Uses Cloudflare if configured, falls back to ngrok, then localhost
# - ngrok: Use ngrok tunnel (requires NGROK_DOMAIN)
# - cloudflare: Use Cloudflare Tunnel (requires CLOUDFLARE_DOMAIN)
# - none: Use localhost (Signal won't work with localhost)
TUNNEL_PROVIDER=auto

# ============================================
# Cloudflare Tunnel Configuration (Recommended - Free)
# ============================================
# Signal doesn't allow localhost, so we use Cloudflare Tunnel for development.
# See cloudflare/README.md for setup instructions.

# Enable Cloudflare Tunnel mode (true/false)
CLOUDFLARE_ENABLED=true

# Your Cloudflare Tunnel hostname
# Quick tunnel: random-words.trycloudflare.com
# Named tunnel: signal-dev.yourdomain.com
CLOUDFLARE_DOMAIN=

# ============================================
# Ngrok Configuration (Alternative - Paid for TLS)
# ============================================
# Note: ngrok TLS tunnels now require pay-as-you-go.
# See ngrok/README.md for setup instructions.

# Enable ngrok tunnel mode (true/false)
NGROK_ENABLED=false

# Your ngrok domain (get from ngrok dashboard after starting tunnel)
# Example: abc123xyz.ngrok.io or your-reserved.ngrok.io
NGROK_DOMAIN=

# ============================================
# Domain Settings
# ============================================
# When a tunnel is enabled and configured:
#   - DOMAIN defaults to the tunnel domain
#   - BASE_URL defaults to https://${DOMAIN}
# Override explicitly if needed:
# DOMAIN=your-custom-domain.com
# BASE_URL=https://your-custom-domain.com

# ============================================
# Debug Settings
# ============================================
# Debug mode (true/false)
DEBUG=true

# Log level: debug, info, warn, error
LOG_LEVEL=debug
