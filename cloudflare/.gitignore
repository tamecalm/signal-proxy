# Cloudflare Tunnel - Git Ignore
# Ignore sensitive configuration and credential files

# Tunnel credentials (contains secret)
*.json

# Active configuration (may contain tunnel ID)
config.yml

# Certificate files
*.pem
cert.pem
